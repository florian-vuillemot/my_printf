/*
** test.c for test in /home/vuille_f/PSU_2015_my_printf/string
** 
** Made by Florian Vuillemot
** Login   <vuille_f@epitech.net>
** 
** Started on  Sat Nov  7 15:39:27 2015 Florian Vuillemot
** Last update Sat Nov  7 17:00:10 2015 Florian Vuillemot
*/

#include		"struct_string.h"

int			main(int ac, char **av)
{
  t_string		*str;
  t_string		*str_bis;

  str_bis = str = NULL;
  while ((ac = ac - 1) > 0)
    {
      str = add_string(str, av[ac]);
      str_bis = conca_string(str_bis, str);
    }
  print_string(str);
  printf("\n");
  str = remove_elem_to_string(str, 0);
  print_string(str);
  printf("\n");
  free_string(str);
  free_string(str_bis);
}
